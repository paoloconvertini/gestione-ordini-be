version: '3.8'

services:
  sqlserver:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: ostuni-db-api
    environment:
      ACCEPT_EULA: "Y"
      SA_PASSWORD: "Pass@word"
    ports:
      - "1434:1433"
    volumes:
      - ostuni:/var/opt/mssql

volumes:
  ostuni:
